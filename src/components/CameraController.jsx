// CameraController.jsx
//
// Caméra libre pour la scène (OrbitControls, via @react-three/drei ou
// three/examples/jsm/controls/OrbitControls). Pas de logique d'avatar ni de
// déplacement de personnage pour l'instant : uniquement le contrôle de la
// caméra par l'utilisateur (orbite/zoom/pan).
//
// À utiliser à l'intérieur du Canvas r3f orchestré par IslandScene.

export default function CameraController() {
  // TODO: instancier <OrbitControls /> (ou FlyControls) et exposer les
  // réglages nécessaires (limites de zoom, damping, etc.)
  return null;
}
