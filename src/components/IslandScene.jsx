// IslandScene.jsx
//
// Conteneur Three.js / @react-three/fiber de la scène principale : une île
// flottante qui héberge les cartes affichées sous forme d'étoiles/objets
// interactifs. Orchestre CameraController (caméra libre), SkyScene (le ciel
// étoilé) et SkyAtmosphere (couleur ambiante selon le type sélectionné).
//
// Props attendues :
//   - cards       : tableau des cartes à représenter dans la scène
//   - onStarClick : callback(card) déclenché au clic sur une étoile/carte

export default function IslandScene({ cards, onStarClick }) {
  // TODO: implémenter le Canvas r3f, le mesh de l'île flottante et le
  // placement des cartes, puis brancher CameraController / SkyScene /
  // SkyAtmosphere à l'intérieur du Canvas.
  return null;
}
